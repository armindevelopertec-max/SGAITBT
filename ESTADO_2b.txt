=============================================================
ESTADO DE TRABAJO — SGA ITBT (corte durante Fase 2b)
Fecha: 2026-09-16  ~00:10
Proyecto: /home/armintec/Proyectos2026/SGAITBT
=============================================================

DATOS CLAVE
- .env raíz: /home/armintec/Proyectos2026/SGAITBT/.env (DB_USER, DB_NAME, DB_PASSWORD, DB_PORT)
- Acceso BD: docker exec sga_postgres psql -U $DB_USER -d $DB_NAME
- Backend :3001 (nest start --watch) — ACTUALMENTE NO CORRIENDO
- Frontend :3000 — corriendo
- Login: admin/admin2026 · Docentes jquispe/mchoque/carana/lflores/demo2026
- Student test EST: 9876543/demo2026
- Throttle login: 5 por 60s por IP (esperar 12s entre logins en smoke)

=============================================================
FASE 1 — COMPLETA (aprobada por usuario)
- Roles recalibrados (APOYO=only dashboard.view; ESTUDIANTE=dashboard.view+history.view)
- seedRoles() reconcilia roles de sistema al arrancar (borra role_permissions obsoletas)
- Matrices OR en 11 controllers + /users/role/:role
- Guard frontal de rutas en (dashboard)/layout.tsx + permisos.frontend sin fallback legado
- Tests backend 19/19, e2e 3/3, tsc+lint ok; frontend tsc+lint ok (solo warnings previos)

=============================================================
FASE 2a — COMPLETA (quitar users.role)
- Se quitó columna role de la entidad User, enums, DTOs, service (RBAC-only),
  auth (role derivado = authorities.roles[0]), seed (sin persistir role)
- Tests 19/19 ok, columna role ya eliminada en BD dev

=============================================================
FASE 2b — EN CURSO (SubjectAssignment: teacher_id(User) -> employee_id(Employee DOCENTE))
=============================================================

CAMBIOS YA APLICADOS Y VERIFICADOS (tsc+lint+19 tests OK ANTES de tocar la BD):

[Backend]
1) subject-assignment.entity.ts
   - teacher: User + teacher_id  ->  employee: Employee + employee_id (uuid nullable)
   - nuevo @Index('IDX_assignment_employee_id')

2) subject-assignment.dto.ts
   - Create/Update/QueryDto: teacherId -> employeeId

3) subject-assignment.service.ts
   - Inyectado EmployeeRepository
   - create()/update(): valida que employeeId exista (BadRequest si no)
   - findAll/findOne: relations employee + employee.persona; empieza por employeeId
   - findByTeacher -> findByEmployee(employeeId)

4) subject-assignment.controller.ts
   - GET teacher/:teacherId -> teacher/:employeeId (método findByEmployee)

5) subject-assignment.module.ts
   - Employee agregado a TypeOrmModule.forFeature

6) employee.controller.ts
   - GET /employees y GET /employees/:id -> @RequirePermission(EMPLOYEES_VIEW, ASSIGNMENTS_VIEW)
     (para que DOCENTE/COORDINADOR/SECRETARIA puedan cargar lista de docentes)

7) seed.ts (líneas ~388-405 y ~586)
   - teacherUsers -> teacherEmployees (Employee)
   - Tras ensureUser, busca el EMPLOYEE por persona del user (DOCENTE)
   - Asignaciones: teacherId: teacherUsers[...].id -> employeeId: teacherEmployees[...]?.id

[Frontend]
8) lib/types.ts: SubjectAssignment.teacherId/teacher -> employeeId/employee (Employee)
9) assignments/page.tsx:
   - apiGet<User[]>('/users/role/TEACHER') -> apiGet<Employee[]>('/employees?employeeType=DOCENTE')
   - form.teacherId -> form.employeeId; select y tabla usan employee.persona
   - helper personName() inline (firstName+paternalSurname+maternalSurname, fallback lastName)
10) reports/page.tsx: a.teacher?.fullName -> personName(a.employee.persona) + helper + useEffect intacto
11) centralizers/page.tsx: selected.teacher?.fullName -> personName(selected.employee.persona) + helper

VERIFICACIÓN HECHA: backend tsc OK · backend lint OK · frontend tsc OK · frontend lint OK (8 warnings pre-existentes) · tests backend 19/19 OK

=============================================================
ESTADO DE LA BD en el corte (IMPORTANTE)
- El backend se quedó SIN servidor (watcher estancado; se mató).
- Se relanzó manualmente UNA vez; el synchronize de TypeORM falló en bucle:
    ALTER TABLE subject_assignments ADD CONSTRAINT FK(...employee_id) REFERENCES employees
    -> "insert or update ... violates foreign key ..." (luego rollback de sync completo)
- Se hizo manualmente:
    ALTER TABLE subject_assignments DROP CONSTRAINT "FK_f705d08aa7a522b5ebb70dc5f22";  (FK teacher_id->users)
    ALTER TABLE subject_assignments DROP COLUMN teacher_id;
- ESTADO ACTUAL: tabla subject_assignments SIGUE SIN columna employee_id (falta que synchronize la cree).
- Limpieza: delete (si existe) /tmp/opencode/backend.log

=============================================================
LO QUE FALTA PARA TERMINAR 2b
A. Relanzar backend y dejar que synchronize cree employee_id + FK:
   cd /home/armintec/Proyectos2026/SGAITBT/backend
   nohup npm run start:dev > /tmp/opencode/backend.log 2>&1 &
   sleep 25 && ss -ltnp | grep 3001   (verificar escucha)
   Verificar en BD:
   docker exec sga_postgres psql -U $DB_USER -d $DB_NAME \
     -c "\d subject_assignments"   (debe aparecer employee_id + FK a employees, y NOTA: la FK y el índice IDX_assignment_employee_id)
   Si vuelve a fallar el sync, revisar /tmp/opencode/backend.log.
   Nota: las filas existentes quedan con employee_id NULL (OK; se reseedeará el rebuild total al final).

B. Sql opcional para smoke (asignar un docente a asignaciones existentes):
   UPDATE subject_assignments sa SET employee_id = (
     SELECT e.id FROM employees e
     JOIN persons p ON p.id=e.person_id
     JOIN users u ON u.persona_id=p.id
     WHERE e.employee_type='DOCENTE' LIMIT 1)
   WHERE sa.employee_id IS NULL;

C. Smoke de 2b (re usar /tmp/opencode/smoke_phase1.sh o manual) con DOCENTE (jquispe) y SECRETARIA:
   - login admin, secretaria, jquispe
   - GET /subject-assignments -> 200 y cada item trae employee.persona
   - GET /employees?employeeType=DOCENTE -> 200 para DOCENTE (antes 403; ahora permite ASSIGNMENTS_VIEW)
   - GET /subject-assignments/teacher/{employeeId} -> 200
   - GET /employees (sin filtro) -> DOCENTE/SECRETARIA deben poder ver (OR ASSIGNMENTS_VIEW)
   - POST /subject-assignments con employeeId de un docente -> 201 y se ve en el GET
   - POST /subject-assignments con employeeId inventado -> 400 "El docente no existe"
   - GET /users/role/TEACHER debe SEGUIR funcionando (no se rompió; RBAC)

D. Probar la página en el navegador (localhost:3000/assignments) con admin y con docente.

E. Pendientes GENERALES de la Fase 2 (cuando 2b quede OK):
   2c) Student -> Person (dedupe de datos personales; Student conserva académico + personaId)
       - Afectaría: student.entity/dto/service/controller, person module, seeds, frontend
         (types.ts Student, lists que usan student.firstName/lastName/ci: assignments page, students page,
          reports, history, grades, attendance, centralizers...)
   - Regenerar baseline migration 1789529500326-Baseline.ts desde esquema final
     (borrar la antigua, validar en BD limpia: migrations + seed)
   - Verificación completa + checkpoint antes de Fase 3

F. Fases siguientes (guion global) — ESPECIFICACIÓN DETALLADA:

----------------------------------------------------------
FASE 3 — Sesión HttpOnly + Refresh Token (auth)
----------------------------------------------------------
OBJETIVO: sacar el token de localStorage (hoy 'sga_token' en frontend/src/lib/session.ts,
enviado como "Authorization: Bearer" en frontend/src/lib/api.ts:12-14) y usar cookie
HttpOnly + tokens de acceso cortos con refresh.

ESTADO ACTUAL (archivos base):
- backend/src/modules/auth/auth.service.ts: login() firma UN solo JWT de acceso con
  JwtPayload { sub?, id, username, email, role?, roles[], permissions } vía jwtService.signAsync.
  validateUser() + getAuthorities() cargan roles/permisos (RBAC).
- backend/src/modules/auth/strategies/ (JwtStrategy): lee Bearer del header por defecto.
- backend/src/common/guards/jwt-auth.guard.ts: JwtAuthGuard estándar (passport-jwt).
- frontend/src/lib/session.ts: getToken/setToken/getUser/setUser/clearSession con localStorage.
- frontend/src/lib/api.ts: apiGet/apiPost/apiPatch/apiDelete adjuntan header y NO usan credentials.
- Login page: frontend/src/app/login/page.tsx (hace POST /auth/login y luego setToken).

ENTREGABLES:
1) access token CORTÍSIMO (ej. 15 min) + refresh token (ej. 7-30 días), los dos en cookie
   HttpOnly (path=/api o según ámbito; SameSite=Lax; Secure en producción).
   - Mantener JWT firmado (kJWT_SECRET) para access; refresh puede ser JWT con "type":"refresh"
     con distinto secreto/duración, o un token aleatorio en BD (tabla refresh_tokens) — DECISIÓN
     a validar: prefiero token aleatorio en BD (revocable) para control de logout activo.
2) Endpoints nuevos en auth.controller:
   - POST /auth/refresh  -> rota el refresh (emite access+refresh nuevos), valida firma/vigencia/
     revocación, y el usuario sigue activo.
   - POST /auth/logout   -> revoca el refresh (si es en BD) y borra cookies.
   - Respuesta de /auth/login y /auth/refresh: Set-Cookie HttpOnly (los 2 tokens) + payload
     de usuario (roles/permissions/role primario) como JSON (no sensible).
   - GET /auth/me sigue existiendo y deja de depender de JwtPayload.role (usar roles[]).
3) Backend: estrategia JWT pasa a leer cookie (extraer access de la cookie; si no hay o falla ->
   401 y el frontend llama a /auth/refresh una vez). Añadir guard/strategy para refresh.
   - Mantener compatibilidad con header Bearer en desarrollo (opcional, para curl/smoke).
4) Frontend:
   - api.ts: fetch con credentials:'include'; quitar header Authorization manual y getToken().
   - session.ts: guardar solo 'sga_user' (payload de usuario sin secretos); getToken() deja de
     existir o devuelve null; clearSession borra user y llama POST /auth/logout.
   - Lógica de 401 -> intentar /auth/refresh una vez y reintentar el request; si falla -> login.
   - auth.tsx (useAuth): exponer datos del usuario desde sga_user + refrescar /auth/me al cargar.
   - login/page.tsx: sin setToken local; esperar cookies + user payload.
5) CSRF: al usar cookie, evaluar protección (doble cookie / SameSite / header custom). MÍNIMO:
   SameSite=Lax + verificar Header X-CSRF o 'X-Requested-With' en endpoints mutantes.
   (No introducir debilidad: decidir explícitamente y anotar en el checkpoint.)
6) Verificación: login (Set-Cookie), profile con curl -b/-c, refresh no autorizado -> 401,
   logout revoca, paso de fases/checks manuales de sesión en navegador, y actualizar
   e2e (test/e2e) que hoy probablemente usan header Bearer.
   NOTA: smoke_phase1.sh/usar credenciales y throttle 5/60s.

----------------------------------------------------------
FASE 4 — Autoasignación por paralelo, Dashboard de docente, EGRESADO->TITULADO, scoping
----------------------------------------------------------
A. Autoasignación con paralelo (bug pendiente):
   - subject-assignment.service.autoAssignStudents() (backend/.../subject-assignment.service.ts:107)
     localiza la asignación SOLO por subjectId+academicPeriodId, ignora dto.parallel.
     hoy frontend/assignments/page.tsx 'Autoasignar' no manda paralelo.
   - CORREGIR: si dto.parallel viene presente, (1) buscar asignación con ese paralelo;
     si no existe y hay permiso ASSIGNMENTS_CREATE, crearla; (2) en el bucle de matrícula
     repartir/agrupar por paralelo de forma determinista (ej. round-robin por matrícula
     según número) y crear la asignación del paralelo "B" cuando se sobrepasa cupo.
   - DTO AutoAssignStudentsDto ya tiene parallel?: string (dto/subject-assignment.dto.ts).
   - Actualizar frontend para enviar parallel desde la columna del assignment.
   - Verificar con e2e/tests: auto-assign dividiendo estudiantes entre A y B.

B. Dashboard real de docente:
   - HOY: dashboard.controller GET /dashboard/teacher llama getTeacherDashboard(user.id)
     pero el servicio FIRMA recibe _teacherId y no lo usa (dashboard.service.ts:120) -> mock.
   - HACER: getTeacherDashboard(employeeId) con datos reales del DOCENTE:
     * empleado del usuario: users.persona -> employees (Employee DOCENTE). Endpoint/servicio:
       "me" -> obtener employee del user autenticado.
     * asignaciones del docente (SubjectAssignment.employeeId), materias, paralelos, gestión.
     * nº de estudiantes por asignación (subject_enrollments), calificaciones ingresadas,
       asistencia registrada, próxima clase/schedule.
   - Frontend: la ruta 'dashboard/teacher' (ver frontend/src/app/(dashboard)/page.tsx:33 que
     redirige a '/dashboard/teacher') debe consumir /dashboard/teacher. Si la página no existe,
     CREARLA (tarjetas con totales + lista de materias/paralelos + enlaces a grades/attendance).
- Requiere que assignments entienda employee (ya hecho en 2b) y probablemente un helper
      "employee de mi usuario" (GET /employees/me o resolver por CurrentUser en el controller).

C. Scoping server-side para ESTUDIANTE (pendiente desde Fase 1):
   - HOY por diseño OR consolida lectura, pero ESTUDIANTE con history.view puede consultar
     /academic-history/:studentId de CUALQUIER estudiante.
   - HACER: en academic-history.controller y grade.controller, si el rol principal es
     ESTUDIANTE (authorities.roles), forzar studentId == user.studentId (403/404 si no
     coincide). ADMIN/SECRETARIA/DOCENTE/etc. siguen igual.
   - Frontend academic-history/page.tsx ya usa /students/me cuando no hay students.view:
     mantener, pero ahora también el servidor lo blinda.

D. EGRESADO -> TITULADO:
   - Ya existe frontend/src/app/(dashboard)/graduation (página) y estados de Student (enums).
   - Definir el recorrido de estados: ESTUDIANTE -> EGRESADO -> TITULADO.
   - Entregables a confirmar con el cliente: endpoints (POST /graduates/:studentId o
     /graduates/register con fecha, nº de diploma, acta; @Audit), entidad Graduation/
     Titulation + tabla, estado de student actualizado transaccionalmente, y la página
     graduation consumiéndolos.
   - Manejar duplicados / requisitos (materias aprobadas, nota mínima, deuda de pagos)
     si aplican: decidir en el checkpoint.

----------------------------------------------------------
FASE 5 — MinIO: URLs presignadas (reemplazar multipart directo)
----------------------------------------------------------
ESTADO ACTUAL (módulo ya existe):
- backend/src/modules/minio/{minio.module.ts,minio.service.ts,minio.controller.ts}
- minio.controller.ts: POST /uploads/photo|voucher|logo|document con FileInterceptor
  (multipart), valida MIME/tamaño (foto 5MB, doc 10MB) y llama minioService.uploadFile
  (sube buffer y devuelve { url, objectName }).
- Buckets en minio.service (BucketName.PHOTOS/VOUCHERS/LOGOS/DOCUMENTS).
- Permisos: FILES_UPLOAD (y INSTITUTION_UPDATE en logo). Frontend lo usa en
  students/persons/deposits/institution (photoUrl, vouchers, logo).

OBJETIVO: no mandar binarios por el API; generar URL presignada PUT para subir directo
a MinIO y URL presignada GET (expiración) para ver/descargar evitando streaming por el
backend y limitando exposición de objetos.

ENTREGABLES:
1) minio.service:
   - presignedPutUrl(bucket, objectName, contentType, expiresInSeconds) y
     presignedGetUrl(bucket, objectName, expiresInSeconds).
   - Mantener uploadFile() actual para compat/tests o deprecarlo.
   - POST /uploads/presign/{bucket}: valida permiso + tipo/tamaño esperado del bucket,
     genera objectName (UUID + extensión sanitarizada) y devuelve
     { putUrl, getUrl, objectName }.
   - GET /uploads/url?objectName=&bucket=&expires= -> presigned GET (foto/logo/voucher)
     con permiso de lectura apropiado.
2) Seguridad:
   - Restringir bucket por endpoint (photo/voucher/logo/document); validar extensión,
     contentType y tamaño coherente con el bucket.
   - No exponer objetos de terceros: el presign GET debe verificar que el solicitante
     tiene permiso sobre la entidad (persona/depósito) o rol con reports.view
     (vouchers en reports/concentrados). Anotar decisión en checkpoint.
3) Frontend:
   - Donde hoy se usa multipart (inputs de file en students/persons/deposits/institution):
     llamar POST /uploads/presign/..., subir directo con fetch PUT al putUrl, luego guardar
     objectName/urls vía API normal.
   - Helper reutilizable (lib/uploads.ts).
4) Verificación: subida real directa a MinIO (docker), render con presigned GET,
   expiración de URL, cleanup de objetos huérfanos si procede.

----------------------------------------------------------
FASE 6 — Ampliar @Audit a todos los endpoints mutantes
----------------------------------------------------------
ESTADO ACTUAL:
- backend/src/common/decorators/audit.decorator.ts (AuditMetadata: module/action/
  entityType/entityIdFrom 'params'|'body'/description) + AuditInterceptor en
  common/interceptors.
- Módulo audit ya existe: backend/src/modules/audit/{audit.controller,audit.service,
  audit.module,dto,entities} (endpoint de consulta + entidad de auditoría).
- HOY solo algunos create/update/delete están anotados.

OBJETIVO: cobertura 100% de endpoints mutantes (POST/PATCH/PUT/DELETE) con registro de
autor, acción, entidad, id, payload normalizado (o diff) y consulta por rol.

ENTREGABLES:
1) Inventario: grepear @Post/@Patch/@Put/@Delete en backend/src/modules/*/*controller.ts
   y listar cuáles no tienen @Audit.
2) Anotar con parámetros correctos (entityIdFrom params/body, entityIdKey según entidad;
   en arrays de ids registrar todos). Módulos típicos: student, person, enrollment,
   deposit, grade, attendance, subject-assignment (incluido enroll-student/remove-student),
   subject, career, academic-period, institution, user (+cambios de roleKeys/roles), rbac
   (asignación de roles/permisos si existe), graduation (Fase 4).
3) audit.service: resolver entityId del body/params; almacenar payload normalizado
   EXCLUYENDO campos sensibles (passwordHash, secretos). Respetar soft deletes.
4) audit.controller: revisar permiso (¿audit.view? ya debe existir en permissions.ts) y
   paginación; restringir consulta a ADMIN/SECRETARIA/DIRECTIVO según ROLE_TREE.
5) Verificación: crear/editar/borrar en cada módulo, confirmar fila en tabla audit + API,
   tests.

----------------------------------------------------------
ORDEN RECOMENDADO / DEPENDENCIAS
- Fase 3 (auth) antes que Fase 4C (scoping) para usar bien el rol en CurrentUser.
- Fase 4 (dashboard/auto-assign/egresados) depende de 2b terminado (employee_id).
- Fase 5 es independiente (puede ir en paralelo).
- Fase 6 conviene después de Fase 4 para no auditar flujos que cambiarán.
CADA fase termina con: tsc + lint (backend/frontend) + tests (19) + e2e + smoke por rol
+ checkpoint al usuario antes de seguir.

COMANDOS ÚTILES
- Backend: cd backend && npm run start:dev (watch)
- Tests: cd backend && npm test  (4 suites / 19 tests)
- e2e: cd backend && npm run test:e2e
- tsc: backend/frontend: npx tsc --noEmit
- lint: backend/frontend: npm run lint
- Smoke previo: /tmp/opencode/smoke_phase1.sh
- Regenerar migración baseline al final:
    cd backend && npx typeorm-ts-node-commonjs migration:run -d src/config/data-source.ts (tras generar)
=============================================================